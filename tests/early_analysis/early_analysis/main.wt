/// Type uses other type declared later
type A(b: B) {
    b: B = b
}

/// Other type
type B {
}

/// First function use second function declared later
fn first(a: int, b: int): int =
    second(a, b)

/// Second function
fn second(a: int, b: int): int {
    a + b
}

// Empty main
fn main() {
}
