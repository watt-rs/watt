type Flower(water_level: int) {
    pub let water_level: int = water_level
    pub fn water(add: int) {
        self.water_level += add
    }
}

enum Pot {
    Full(flower: Flower),
    Nothing
}

enum Option {
    Some(element: dyn),
    None
}

fn sum(a: dyn, b: dyn): float {
    return a
}

fn main() {
    let a: dyn = sum(13,15)
}
