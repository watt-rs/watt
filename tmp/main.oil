type Flower(amount: int) {
    let water_amount = amount
    pub fn water(amount: int) {
        self.water_amount += amount
    }
}

fn main() {
    let flower: Flower = new Flower(true)
    flower.water(flower.water_amount)
}
